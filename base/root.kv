
<PKOSRoot>:
    BoxLayout:
        orientation: 'vertical'
        pos: root.pos
        # pos: root.pos[0] - 20, root.pos[1] + 20
        size: root.size
        size_hint: (None, None)

        canvas:
            PushMatrix:
            Color:
                rgba: bg_dark
            Rectangle:
                pos: self.pos
                size: self.size
            PopMatrix:


        ContextTabRow:
            id: tabs
            size_hint: (1, None)
            size: 0, 25


        ContextView:
            id: view
            # size_hint: (1, 1)

